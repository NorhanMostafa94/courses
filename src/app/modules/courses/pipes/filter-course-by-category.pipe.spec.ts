import { FilterCourseByCategoryPipe } from './filter-course-by-category.pipe';

describe('FilterCourseByCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCourseByCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
